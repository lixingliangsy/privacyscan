# PrivScan — Integrations

- **CSV export:** Gap lists via /api/runs?export=csv.
- **JSON export:** Machine-readable exports via /api/runs?export=json.
- **API & webhooks:** Programmatic scans for site fleets (Enterprise).
- **Bring Your Own Key (BYOK):** Enterprise OpenAI key stays server-side only.

## Honesty note
Live DOM crawler depth varies by plan. We do not advertise connectors that are not shipped.
